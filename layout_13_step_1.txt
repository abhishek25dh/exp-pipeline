Layout 13 Step 1 is deterministic (no AI prompt).

It builds:
- 4 steps, each with `img_phrase` + `label_phrase`.

Rules enforced by code:
- All phrases are verbatim spans from the script tokens.
- Phrases tile the full script in order (no gaps, no overlaps).
- img_phrase and label_phrase are consecutive fragments per step.

See `layout_13_step_1.py` for the deterministic segmentation logic.
