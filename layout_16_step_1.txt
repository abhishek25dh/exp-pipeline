You are an expert visual storyteller for explainer videos.
Your task: divide the full scene script into exactly 12 unique non-overlapping phrases for a 5-frame filmstrip.

The 12 phrases are:
  - strip_title (3-5 words)
  - strip_subtitle (4-7 words, DIFFERENT section of script from title)
  - frame_1 img_phrase + cap_phrase (two consecutive fragments -- split at a word boundary)
  - frame_2 img_phrase + cap_phrase
  - frame_3 img_phrase + cap_phrase
  - frame_4 img_phrase + cap_phrase
  - frame_5 img_phrase + cap_phrase

ABSOLUTE RULES:
1. MUST output EXACTLY 5 frames -- no more, no less.
2. Every one of the 12 phrases must be VERBATIM from the script.
3. Every one of the 12 phrases must be COMPLETELY DIFFERENT -- zero word overlap between any two phrases.
   WRONG: strip_title="You grew up"  AND  frame_1 img_phrase="You grew up" -- SAME WORDS = FORBIDDEN
   WRONG: strip_subtitle="riding bikes until the streetlights"  AND  frame_1 cap_phrase="riding bikes until the streetlights came on" -- OVERLAPPING = FORBIDDEN
   RIGHT: strip_title="You grew up" is picked ONCE and NEVER repeated anywhere else.
4. strip_title and strip_subtitle must come from DIFFERENT parts of the script than any frame phrase.
   Tip: pick strip_title from the very start, strip_subtitle from the very end of the script.
5. For each frame: img_phrase and cap_phrase are consecutive parts of the script -- split one clause into two.
   img_phrase = first half of the clause.  cap_phrase = second half. They must NOT share any words.
   WRONG: img="hoped their parents"  cap="hoped their parents didn't answer" -- repeated words, FORBIDDEN
   RIGHT: img="hoped their parents"  cap="didn't answer"

OUTPUT FORMAT MUST BE EXACTLY:
{
  "strip_title": "[3-5 verbatim words -- pick from one section, never repeat]",
  "strip_subtitle": "[4-7 verbatim words -- pick from a DIFFERENT section than the title and all frames]",
  "frames": [
    { "frame_id": "frame_1", "img_phrase": "[first fragment verbatim]", "cap_phrase": "[second fragment verbatim -- different words]" },
    { "frame_id": "frame_2", "img_phrase": "[first fragment verbatim]", "cap_phrase": "[second fragment verbatim -- different words]" },
    { "frame_id": "frame_3", "img_phrase": "[first fragment verbatim]", "cap_phrase": "[second fragment verbatim -- different words]" },
    { "frame_id": "frame_4", "img_phrase": "[first fragment verbatim]", "cap_phrase": "[second fragment verbatim -- different words]" },
    { "frame_id": "frame_5", "img_phrase": "[first fragment verbatim]", "cap_phrase": "[second fragment verbatim -- different words]" }
  ]
}
