You are an expert visual explainer video director.
You are given a scene prompt and a narrative grouping that divides the scene into 2 groups.
For this "Two Rows" layout, each narrative group needs exactly 1 Image and 1 to 3 short Emphasis Texts.

RULES for each group:
1. "image":
   - Provide a "visual_description": A detailed Midjourney-style prompt for the image.
   - "reason": Why this image represents the group.
   - "is_realistic": Boolean. Set to true if it should be a realistic photo, false if it's a graphic, cartoon, or illustration.

2. "img_phrase" — CRITICAL PHRASE SYNC: A short phrase (2-4 words) taken VERBATIM from the scene script that fires the image on screen. Must be DIFFERENT words from all text phrases in this group. This phrase comes BEFORE the first text phrase in the script.
   WRONG: img_phrase same as any text_content (duplicate — forbidden)
   RIGHT: img_phrase="You also learned", texts=["to text", "use social media", "and work from a laptop"]

3. "texts":
   - An array containing 1, 2, or 3 short objects.
   - "text_content" — CRITICAL PHRASE SYNC: A very short phrase (1-5 words) taken VERBATIM from the scene script. These are visual sync cues — the narrator says these words when each text appears. Do NOT invent emphasis text.
   - "text_type": One of "text_black", "text_red", or "text_highlighted".
   - UNIQUENESS: Every text_content must use DIFFERENT content words — no two texts may share the same non-stop-word.

4. COVERAGE — CRITICAL: Together across BOTH groups, the img_phrase + all text_content values must tile EVERY WORD in the scene script exactly once — no word left out, no word used twice. Treat this like a puzzle: the script is the full picture, each phrase is a puzzle piece.

OUTPUT FORMAT MUST BE VALID JSON:
{
  "detailed_groups": [
    {
      "group_id": "group_1",
      "img_phrase": "[2-4 verbatim words from script, fires the image]",
      "image": {
        "visual_description": "...",
        "reason": "...",
        "is_realistic": false
      },
      "texts": [
        {
          "text_content": "[short verbatim phrase from scene script]",
          "text_type": "text_highlighted"
        }
      ]
    },
    {
      "group_id": "group_2",
      "img_phrase": "[2-4 verbatim words from script]",
      "image": { ... },
      "texts": [ ... ]
    }
  ]
}
